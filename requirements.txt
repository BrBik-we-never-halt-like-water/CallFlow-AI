# CallFlow AI - backend runtime dependencies for `apps/api`.
#
# Mirrors [project.dependencies] in apps/api/pyproject.toml. Installing the
# package itself (`pip install -e ./apps/api`) resolves the same set; this file
# exists for environments that want a plain requirements install.
#
#   python -m venv .venv
#   .venv/Scripts/activate          # Windows
#   source .venv/bin/activate       # macOS / Linux
#   pip install -r requirements.txt
#
# For tests and linting, use requirements-dev.txt instead - it includes this file.
#
# `apps/voice-runtime` is a separate deployable with its own pyproject and its
# own vendor extras; it is deliberately NOT installed by this file. See
# DEPLOYMENT.md for how that process is installed and run.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-dotenv>=1.0.1
httpx>=0.27.0
rich>=13.9.0

# LiveKit's server API - trunks, dispatch rules, and CreateSIPParticipant.
# Only ever imported by app/integrations/livekit/client.py.
livekit-api>=1.0.0

# Persistence and identity (Supabase Postgres).
asyncpg>=0.30.0
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
psycopg[binary]>=3.2.0
pyjwt[crypto]>=2.9.0

# Encrypting org-owned provider credentials at rest (app/core/crypto.py).
cryptography>=43.0.0
