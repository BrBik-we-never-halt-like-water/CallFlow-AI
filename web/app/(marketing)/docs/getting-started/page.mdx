export const metadata = {
  title: "Getting started",
  description:
    "From an empty account to a dry run with typed results, in about five minutes. Nothing here dials anyone.",
};

# Getting started

This walkthrough ends with you looking at a typed result from a call that was never
placed. Nothing in it spends a credit or rings a phone.

## 1. Add a few contacts

Open **Contacts → Import CSV**, or paste straight from a spreadsheet. Three rows is
enough to start.

The columns recognised are `name`, `phone`, and `note`. A header row is optional;
without one, columns are read in that order.

```text
name,phone,note
Aditi Sharma,+15555550100,asked about Bali in December
Rahul Verma,+15555550101,honeymoon package enquiry
Priya Nair,+15555550102,family trip to Singapore
```

Every row is validated before anything else happens. Numbers must be E.164 — a
leading `+` and a country code — and a bare ten-digit number is assumed to be in your
default region. Rows that fail are flagged with the reason inline and are never
silently dropped.

Those sample numbers are reserved fictional ones in the `+1 555 0100–0199` range.
They cannot connect to a real person, which is why the sample data uses them.

## 2. Pick a campaign

A campaign is two things: a goal written in plain English, and the fields you want
back from every call.

Start with one of the built-in templates. They are marked `TEMPLATE` and cannot be
edited in place — duplicate one if you want to change it. Reading a template's goal
is also the fastest way to understand what a good goal looks like; see
[Writing a good goal](/docs/writing-a-good-goal).

## 3. Run it in dry mode

Go to **Runs → New run**. Dry run is already on, and the panel says so:
`DRY RUN · NO CREDITS SPENT`.

A dry run walks the entire pipeline:

- every row is validated
- the safety gate runs, exactly as it would on a live call
- the goal is rendered with each contact substituted in, so you see the actual words
- a sample result is passed through the real triage logic

What you get back has the same shape a live call produces. The lamps are ice, because
ice means simulated.

## 4. Read the result

Open any row. You get the conversation on the left and, on the right, the extracted
fields plus the triage decision as a short chain — `Frustration detected → Needs a
person`.

That chain reads from typed fields, never from prose. It is the guarantee the product
makes: the reason a call was escalated is a value you can check, not a sentence
somebody wrote.

## 5. Going live

When you are ready, turn the dry-run switch off. It is the only switch in the product
that asks you to confirm, and the confirmation tells you how many contacts are in the
run, the estimated credits, your calling window, and whether the allowlist is on.

Before that first live run, do two things:

1. Put **your own number** on the allowlist. While the allowlist has anything on it,
   nothing else can be dialled.
2. Set the **per-run ceiling** low — three or five. You can raise it once you have
   seen a real call go through.

Both live in [Safety configuration](/docs/safety-configuration).
